# Flutter build output. `flutter test` writes build/ (unit_test_assets, a
# kernel cache, a native-assets manifest), none of which is source and all of
# which is regenerated by the next run. It was untracked-but-not-ignored until
# now, which is the state that gets a directory committed by an `add -A`.
build/
.dart_tool/
