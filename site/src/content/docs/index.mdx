---
title: sqlb
description: Declarative Postgres for Go — a schema is ordinary Go, and migrations, typed models, composable queries, REST handlers, an OpenAPI document and generated clients are all derived from it.
template: splash
hero:
  tagline: >
    Declarative Postgres for Go. A schema is ordinary Go, and migrations, typed
    models, composable queries, REST handlers, an OpenAPI document and clients
    for TypeScript, Dart and the command line are all derived from it — not
    written by hand, so never out of step.
  actions:
    - text: Quickstart
      link: /sqlb/start/quickstart/
      icon: right-arrow
    - text: Read the concepts
      link: /sqlb/concepts/
      icon: open-book
      variant: minimal
    - text: View on GitHub
      link: https://github.com/mind-vm/sqlb
      icon: external
      variant: minimal
---

import { Card, CardGrid, LinkCard } from "@astrojs/starlight/components";

## One declaration, five surfaces

A schema is ordinary Go values, and it is the source of truth for all of these.
Each one is independently optional — a project can take the query builder and
nothing else.

<CardGrid>
  <Card title="Schema" icon="seti:config">
    Tables as Go values: columns, references, indexes, constraints, and the
    capabilities that decide what the outside world can reach.

    [Declaring tables →](/sqlb/schema/)
  </Card>
  <Card title="Queries & domain logic" icon="seti:go">
    A query is a value, so predicates compose on a branch. Hooks are where the
    rules live — one registration constrains every read of a model.

    [Queries →](/sqlb/queries/)
  </Card>
  <Card title="REST API" icon="cloud-download">
    List, read, create, patch and delete per exposed table, with filtering,
    sorting, search, pagination and an OpenAPI document built from the schema.

    [Mounting resources →](/sqlb/rest/)
  </Card>
  <Card title="TypeScript SDK" icon="seti:typescript">
    `where` admits only filterable columns with the operators their type
    accepts, `select` narrows the response type, and a hidden column has no
    spelling at all.

    [TypeScript SDK →](/sqlb/typescript/)
  </Card>
  <Card title="Go CLI" icon="seti:shell">
    A cobra command tree over the same vocabulary, so `--help` states what a
    resource accepts without sending a request — the form the guarantee has to
    take for a caller with no compile step.

    [Go CLI →](/sqlb/cli/)
  </Card>
  <Card title="Migrations" icon="seti:db">
    A schema edit diffed into DDL your own runner applies — and an existing
    database read back into a schema file, which is the same machinery pointed
    the other way.

    [Migrations →](/sqlb/migrations/)
  </Card>
</CardGrid>

## The same query, written twice

Static query generators cannot express *"this WHERE clause exists only when the
user typed something in the search box."* The usual workaround is string
concatenation, which is why the HTTP layer of a filter/sort/search page is
mostly boilerplate.

PostgREST solves that by making the database the API, but there is then nowhere
to put Go domain logic, and the whole schema sits one policy mistake away from
being public.

sqlb takes the middle path. A query is a **value**, so predicates can be added
conditionally:

```go
q := sqlb.Query[Post]().Where(sqlb.F("status").Eq("published"))
if search != "" {
    q = q.Where(sqlb.F("title").Contains(search))
}
posts, err := q.OrderBy(sqlb.F("created_at").Desc()).Limit(50).All(ctx, db)
```

and the REST filter grammar compiles into that **same** predicate AST:

```
?status=eq.published&title=contains.postgres&sort=-created_at&per_page=50
```

One compiler, one bind-parameter discipline, one set of hooks — two producers.
That is the whole design, and
[One grammar, two producers](/sqlb/concepts/one-grammar/) is the long version.

## What that buys

<CardGrid>
  <Card title="Capabilities are opt-in" icon="approve-check">
    `Filterable`, `Sortable`, `Searchable`, `Hidden`. A column that does not
    declare a capability cannot be reached through it — ever. The failure is a
    400 naming what would have worked, not a leak.
  </Card>
  <Card title="Hooks are the domain seam" icon="puzzle">
    `BeforeQuery` receives the query itself, so one registration constrains every
    read of a model — including the ones generated REST handlers issue. Tenant
    scoping stops being something each call site remembers.
  </Card>
  <Card title="Nothing runs unasked" icon="magnifier">
    `SQL()` renders text and args without executing. `Explain` plans against the
    live schema without running. `Diff` returns changes as values; your runner
    applies them.
  </Card>
  <Card title="No dependencies to inherit" icon="seti:go">
    The engine depends on the standard library alone, and a check in CI enforces
    it. Only the REST adapter pulls in Huma, and only if you use it.
  </Card>
</CardGrid>

## Read one that works

Six worked applications, each settling a different question — and each saying
out loud what it deliberately is not.

<CardGrid>
  <LinkCard
    title="blog — the smallest whole thing"
    description="A worked schema, everything codegen emits from it, and the two hand-written pieces generated code cannot produce."
    href="/sqlb/examples/blog/"
  />
  <LinkCard
    title="tasks — the whole surface"
    description="Six tables, JWT auth, a workspace boundary held entirely by hooks, a generated TypeScript client and CLI."
    href="/sqlb/examples/tasks/"
  />
  <LinkCard
    title="library — a finite resource"
    description="No authentication at all. The last copy of a book is lent exactly once, under twenty concurrent borrows."
    href="/sqlb/examples/library/"
  />
  <LinkCard
    title="All six, compared"
    description="Including the same lending library built twice on sqlc, so the trade is visible rather than argued."
    href="/sqlb/examples/"
  />
</CardGrid>

## How this documentation is organized

Four kinds of page, and knowing which you want saves reading the wrong one:

| | |
|---|---|
| **[Start here](/sqlb/start/)** | Takes you through it step by step, from `go get` to a running server. Begin here if you have not used sqlb before |
| **[Concepts](/sqlb/concepts/)** | The reasoning everything else assumes — why a query is a value, why capabilities are opt-in. Five pages, one idea each, no API detail |
| **[How-to](/sqlb/howto/)** | Recipes, indexed by the task you arrived with rather than the surface that owns it: adopting an existing database, moving one sqlc endpoint, renaming a column without breaking clients |
| **[Reference](/sqlb/reference/)** | Exactly what each thing accepts, as tables rather than prose — every filter operator, every column type, every rejection. Assumes the concepts. A [glossary](/sqlb/reference/glossary/) covers the vocabulary |

Between the concepts and the reference sit the seven surface sections — Schema,
Queries, REST, the two SDKs, the CLI, Migrations — one per thing derived from
the declaration. Each explains its own mechanism, and each is independently
optional.

The API reference is not here: it is the Go doc comments, on
[pkg.go.dev](https://pkg.go.dev/github.com/mind-vm/sqlb), with the compiled
`Example` functions attached to the symbols they document.

## Getting help

- Stuck on a word? The [glossary](/sqlb/reference/glossary/) defines the ones
  sqlb coined.
- Rejected request you did not expect? Every message sqlb can send is in the
  [rejection reference](/sqlb/reference/rejections/).
- Wondering whether this fits your project? [How sqlb
  compares](/sqlb/project/comparisons/) is honest about when not to use it.
- Anything else — bugs, questions, a surface that should move —
  [open an issue](https://github.com/mind-vm/sqlb/issues).

## Status

sqlb is pre-1.0, has one author and no observed consumers. That is the honest
starting position, and no amount of feature work substitutes for elapsed time
under real traffic. See [compatibility](/sqlb/project/compatibility/) for what
the current tag freezes and which surfaces are expected to move, and the
[adoption review](/sqlb/project/review-adoption-readiness/) for an outside read
on what that means in practice.

What *is* proven, and re-checked on every run rather than asserted: CI applies
the generated DDL to a real Postgres 18, reads it back with `introspect`, and
requires the round trip to be a fixpoint; the query path runs through a real
PgBouncer in transaction pooling, because that is the deployed topology; and the
blog example is generated from its schema, so every behaviour test in it is also
a test of the generator's output.

Postgres only. `LISTEN/NOTIFY`, jsonb aggregation and `RETURNING` are all
load-bearing; multi-dialect support would cost the best features.
